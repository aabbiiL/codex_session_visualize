name: CI

on:
  push:
    branches:
      - feature/v1
  pull_request:

jobs:
  tests:
    runs-on: macos-14
    steps:
      - name: Check out repository
        uses: actions/checkout@v5
      - name: Run complete test suite
        run: scripts/test
